from django.db import models
from django.contrib.auth.models import User

# The accounts app primarily uses Django's built-in User model
# Additional user-related models are in students.models (Student, OTP)

# This file is kept for consistency and potential future extensions
